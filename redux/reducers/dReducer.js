// catalogueReducer.js

const initialState = null;

const dReducer = (state = initialState, action) => {
  // Add your reducer logic here

  // Make sure to handle the initialization case
  if (state === undefined) {
    return initialState;
  }

  // Handle other actions if needed

  return state;
};

export default dReducer;
